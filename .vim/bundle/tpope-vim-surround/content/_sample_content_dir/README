This directory and its corresponding ditamap exist only to provide a quick test the build process, both locally and on Jenkins.
They may be deleted by a writer once they confirm that their local and server tools are installed and configured correctly.